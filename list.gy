class ListExample {
   static void main(String[] args) {
 
      List<String> list = new ArrayList<String>();
      list.add("one");
      list.add("two");
      list.add("Three");
      list.add("Four");
      list.remove(2);
      for(String str : list) {
      println(str);
      }
   } 
}

